import SeriesScreen from "@/components/screens/tabs/SeriesScreen";
import { SafeAreaView } from "react-native-safe-area-context";

export default function Series() {
    return (
        <SafeAreaView style={{flex: 1}}>
            <SeriesScreen />
        </SafeAreaView>
    );
}
